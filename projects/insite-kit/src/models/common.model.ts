export enum WebRole {
  USER = 1,
  ADMIN = 2,
}

export enum Access {
  CREATE = 'c',
  READ = 'r',
  UPDATE = 'u',
  DELETE = 'd',
}

export enum Feature {
  USER_DETAIL = 'detail',
  PROFILE = 'profile',
  NOTIFICATION = 'notification',
}
