export enum WebRole {
  USER = 1,
  SYSTEM_USER = 2,
  DEVELOPER = 3,
  ADMIN = 4,
}

export enum Access {
  CREATE = 'c',
  READ = 'r',
  UPDATE = 'u',
  DELETE = 'd',
}

export enum Feature {
  USER_DETAIL = 'detail',
  PROFILE = 'profile',
  NOTIFICATION = 'notification',
}
